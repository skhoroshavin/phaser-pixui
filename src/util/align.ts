/**
 * Text alignment options for text rendering.
 * Values correspond to Phaser's text alignment constants.
 */
export enum TextAlign {
  /** Align text to the left. */
  Left = 0,
  /** Center text horizontally. */
  Center = 1,
  /** Align text to the right. */
  Right = 2,
}
